name: Continuous Integration

on:
  push:
    branches: [develop]
  pull_request:
    branches: [develop]

jobs:
  static-analysis:
    runs-on: ubuntu-latest
    steps:
    - name: Install Flake and Lint
      run: |
        python -m pip install --upgrade pip
        pip3 install flake8
        flake8 .
